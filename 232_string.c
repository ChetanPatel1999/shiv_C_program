// take input from user
#include <stdio.h>
void main()
{
    char name[50];
    printf("enter your name : ");
    scanf("%s", name); // its take only single wod from user
    printf("name = %s\n", name);
}