#include <stdio.h>
void main()
{
    int a, b;
    printf("enter number : ");
    scanf("%d", &a);
    if (a % 2 == 0)
    {
        printf("even ");
    }
    else
    {
        printf("odd");
    }

    printf("enter number b : ");
    scanf("%d", &b);

    int *arr = malloc(40);

    //store data
    // done operation on memory
    // free(arr);

    


}