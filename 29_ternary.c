// wap to check given two integer are same or different.
#include <stdio.h>
void main()
{
    int a, b;
    printf("enter a : ");
    scanf("%d", &a); // 89
    printf("enter b : ");
    scanf("%d", &b); // 34
    a == b ? printf("num are same") : printf("num are different");
}